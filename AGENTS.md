# AGENTS.md — Kepler Lab

As instruções do projeto estão em [`CLAUDE.md`](./CLAUDE.md), o arquivo primário para qualquer agente que trabalhe neste repositório. Leia-o antes de qualquer mudança.

Resumo mínimo: conteúdo em `src/content/` é puro e sem DOM; lógica nova entra como função testada antes do componente; antes de concluir, execute `npm test`, `npm run build` e `npm run lint`.
